const db=require("../config/db");exports.assign=async(req,res)=>{const{memberUserId,workout,nutrition}=req.body;await db.query("INSERT INTO plans(member_user_id,trainer_user_id,workout,nutrition) VALUES(?,?,?,?) ON DUPLICATE KEY UPDATE trainer_user_id=VALUES(trainer_user_id),workout=VALUES(workout),nutrition=VALUES(nutrition)",[memberUserId,req.user.id,workout,nutrition]);res.status(201).json({message:"Plan assigned"})};